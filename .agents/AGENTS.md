# Custom rules for ashwinnithi/greendew

- Do NOT push any changes to `ashwinnithi/Product_listing_site.git` (located in the `repo/` subdirectory). Only push changes to `ashwinnithi/greendew.git` (the main repository).
